# A primeira execução deve ser com internet conectada, para limpar esse arquivo.
# Quando o programa não conseguir se conectar ao URL de perguntas ou URL de checagem, ele vai se referir aos arquivos: .lastcheck e .lastvalue